# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# SPDX-License-Identifier: Apache-2.0
{{- if .Values.global.enableNonClusterRoles }}

{{$name := or .Values.global.name .Release.Name }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $name }}-{{ .Values.services.backendListener.serviceName }}
  namespace: {{ .Values.global.agentNamespace }}
  labels:
    app: {{ $name }}-{{ .Values.services.backendListener.serviceName }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ $name }}-{{ .Values.services.backendListener.serviceName }}
  template:
    metadata:
      labels:
        app: {{ $name }}-{{ .Values.services.backendListener.serviceName }}
        {{- with .Values.services.backendListener.extraPodLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
      {{- if .Values.sidecars.OTEL.enabled }}
        sidecar.opentelemetry.io/inject: "true"
      {{- end}}
      {{- with .Values.services.backendListener.extraPodAnnotations }}
      {{- toYaml . | nindent 8 }}
      {{- end }}

    spec:
      nodeSelector:
        {{- range $k, $v := .Values.global.nodeSelector}}
        {{ $k }}: "{{ $v }}"
        {{- end}}
        {{- range $k, $v := .Values.services.backendListener.nodeSelector}}
        {{ $k }}: "{{ $v }}"
        {{- end}}
      {{- if .Values.global.imagePullSecret }}
      imagePullSecrets:
      - name: {{ .Values.global.imagePullSecret }}
      {{- end }}
      serviceAccountName: {{ include "backend-operator.listener.serviceAccountName" . }}
      {{- with .Values.services.backendListener.initContainers }}
      initContainers:
      {{- toYaml . | nindent 6 }}
      {{- end }}
      hostAliases:
        {{- with .Values.services.backendListener.hostAliases }}
          {{- toYaml . | nindent 8}}
        {{- end }}
      tolerations: {{ toYaml .Values.global.tolerations | nindent 8 }}
      containers:
      - name: backend-listener
        image: {{ .Values.global.osmoImageLocation }}/{{ .Values.services.backendListener.imageName }}:{{ .Values.global.osmoImageTag }}
        imagePullPolicy: {{ .Values.services.backendListener.imagePullPolicy }}
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          runAsNonRoot: true
          runAsUser: 1001
        command:
        - listener
        args:
        - --serviceURL
        {{- if .Values.global.serviceGrpcPort }}
        - {{ .Values.global.serviceUrl }}:{{ .Values.global.serviceGrpcPort }}
        {{- else }}
        - {{ .Values.global.serviceUrl }}
        {{- end }}
        - --tokenFile
        - /opt/osmo/secrets/token.txt
        - --backend
        - {{ .Values.global.backendName }}
        - --namespace
        - {{ .Values.global.backendNamespace }}
        - --nodeConditionPrefix
        - '{{ .Values.global.nodeConditionPrefix }}'
        - --maxUnackedMessages
        - '{{ .Values.services.backendListener.maxUnackedMessages }}'
        - --resyncPeriodSec
        - '{{ .Values.services.backendListener.resyncPeriodSec }}'
        - --stateCacheTTLMin
        - '{{ .Values.services.backendListener.stateCacheTTLMin }}'
        - --usageFlushIntervalSec
        - '{{ .Values.services.backendListener.usageFlushIntervalSec }}'
        - --progressDir
        - '{{ .Values.services.backendListener.progressDir }}'
        - --progressFrequencySec
        - '{{ .Values.services.backendListener.progressFrequencySec }}'
        - --eventChanSize
        - '{{ .Values.services.backendListener.eventChanSize }}'
        - --eventCacheTTLMin
        - '{{ .Values.services.backendListener.eventCacheTTLMin }}'
        - '--enableNodeLabelUpdate={{ .Values.services.backendListener.enableNodeLabelUpdate }}'
        - --labelUpdateChanSize
        - '{{ .Values.services.backendListener.labelUpdateChanSize }}'
        - '--metricsOtelEnable={{ .Values.sidecars.OTEL.enabled }}'
        - --serviceVersion
        - '{{ .Values.services.backendListener.metrics.version | default .Values.global.osmoImageTag }}'
        - --metricsOtelCollectorHost
        - '{{ .Values.sidecars.OTEL.host }}'
        - --metricsOtelCollectorPort
        - '{{ include "backend-operator.otel.getPort" (dict "root" . "portName" "otlp-grpc") }}'
        {{- range $arg := .Values.services.backendListener.extraArgs }}
        - {{$arg | quote}}
        {{- end }}
        env:
        {{- with .Values.services.backendListener.extraEnvs }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        resources:
          {{- toYaml .Values.services.backendListener.resources | nindent 10 }}
        volumeMounts:
        - name: osmo-secret
          mountPath: /opt/osmo/secrets/token.txt
          subPath: token.txt
          readOnly: true
        {{- if .Values.services.backendListener.volumeMounts }}
          {{- toYaml .Values.services.backendListener.volumeMounts | nindent 8 }}
        {{- end }}

        livenessProbe:
          exec:
            command:
            - progress_check
            - --progress_interval
            - "300:300:300:300"
            - --progress_file
            - {{ .Values.services.backendListener.progressDir }}/last_progress_workflow_listener:{{ .Values.services.backendListener.progressDir }}/last_progress_node_usage_listener:{{ .Values.services.backendListener.progressDir }}/last_progress_node_listener:{{ .Values.services.backendListener.progressDir }}/last_progress_event_listener
          failureThreshold: 1
          periodSeconds: 30
          timeoutSeconds: 15
        # Give the container 60 seconds to startup
        startupProbe:
          exec:
            command:
            - progress_check
            - --progress_interval
            - "300:300:300:300"
            - --progress_file
            - {{ .Values.services.backendListener.progressDir }}/last_progress_workflow_listener:{{ .Values.services.backendListener.progressDir }}/last_progress_node_usage_listener:{{ .Values.services.backendListener.progressDir }}/last_progress_node_listener:{{ .Values.services.backendListener.progressDir }}/last_progress_event_listener
          failureThreshold: 12
          periodSeconds: 5
          timeoutSeconds: 15
          initialDelaySeconds: 10

      {{- if .Values.sidecars.OTEL.enabled }}
      {{- include "backend-operator.injectOTEL" . | nindent 6}}
      {{- end }}
      {{- with .Values.services.backendListener.extraSidecarContainers }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      volumes:
      {{- if .Values.services.backendListener.volumes }}
        {{- toYaml .Values.services.backendListener.volumes | nindent 6 }}
      {{- end }}
      - name: osmo-secret
        secret:
          secretName: {{ .Values.global.accountTokenSecret }}
          items:
            - key: {{ .Values.global.accountTokenSecretKey }}
              path: token.txt
      {{- if .Values.sidecars.OTEL.enabled }}
        {{- include "backend-operator.OTELVolumes" (dict "Values" .Values "Prefix" $name) | nindent 6 }}
      {{- end }}
{{- end }}
